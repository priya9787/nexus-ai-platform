version: '3.9'

services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
      
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"  

  redis:
    image: redis:latest
    ports:
      - "6379:6379" 